<?php require "vendor/autoload.php"; $app = require_once "bootstrap/app.php"; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); use PhpOffice\PhpSpreadsheet\IOFactory; $s = IOFactory::load(public_path("assets/import-excel/mapel-template.xlsx")); $sheet = $s->getActiveSheet(); echo json_encode(array_slice($sheet->toArray(null, true, true, true), 0, 2), JSON_PRETTY_PRINT);
